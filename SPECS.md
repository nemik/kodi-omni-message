# Kodi Omni Message

This is a Kodi add-on that allows you to set a message in its config and then whenever any media is tried to be played, it will show that message instead of playing the media. This is useful for parental controls or for displaying a custom message when certain content is accessed.